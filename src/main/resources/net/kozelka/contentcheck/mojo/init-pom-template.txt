            <plugin>
                <groupId>net.kozelka.maven</groupId>
                <artifactId>contentcheck-maven-plugin</artifactId>
                <version>@project.version@</version>
                <executions>
                    <execution>
                        <id>contentcheck</id>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                            <goal>warcc</goal>
                        </goals>
                        <configuration>
                            <toleratedOverlapCount>@overlap.count@</toleratedOverlapCount>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
